import { MathtexDirective } from './mathtex.directive';

describe('MathtexDirective', () => {
  it('should create an instance', () => {
    const directive = new MathtexDirective();
    expect(directive).toBeTruthy();
  });
});
